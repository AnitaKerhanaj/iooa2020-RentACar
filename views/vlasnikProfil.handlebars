<div class="container card text-white bg-dark mb-3 card border-dark mb-3" style="width: 25rem;">
        <img src="{{owner.image}}" class="card-img-top" alt="...">
        <div class="card-body">
            <h5 class="card-title">{{owner.firstname}} {{owner.lastname}}</h5>
            <a href="/chatVlasnik/{{owner._id}}" class="btn btn-primary">Čavrljanje</a>
        </div>
    </div>
</div>

